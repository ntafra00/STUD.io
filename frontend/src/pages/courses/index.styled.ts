import styled from "@emotion/styled"

export const CourseCardsWrapper = styled.div`
    width: 100%;
    display: flex;
    justify-content: space-evenly;
    flex-wrap: wrap;
`

export const CourseCardContainer = styled.div`
    display: flex;
`